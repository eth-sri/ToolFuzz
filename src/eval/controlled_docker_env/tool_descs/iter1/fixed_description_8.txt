Run shell commands on this Linux machine. Input a list of valid shell commands using absolute paths. Ensure correct distinction between files and directories. Validate paths before executing. Invoke the tool multiple times for comprehensive validation of filesystem state.